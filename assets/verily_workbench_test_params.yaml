# Parameters for running the `test` profile on Verily Workbench.
#
# Select this file at the "Set up parameters" step of the Workbench job
# wizard. The Workbench "Output resource bucket" step sets Nextflow's workDir,
# not `outdir`, so `outdir` must be supplied here as a pipeline parameter.
#
# Replace the outdir bucket below with your workspace's output bucket.
input: https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/samplesheet/samplesheet_test_illumina_amplicon.csv
outdir: gs://nf-data-wb-tepid-acorn-3033/nf-core-demo-results
